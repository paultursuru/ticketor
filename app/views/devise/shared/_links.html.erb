<%- if controller_name != 'sessions' %>
  or <%= link_to "log in", new_session_path(resource_name), class:"rounded-full text-white underline underline-offset-8" %><br />
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  or <%= link_to "sign up", new_registration_path(resource_name), class:"rounded-full text-white underline underline-offset-8" %><br />
<% end %>
